var UIDInfo = [];
// get the number of each liver's UID

var liverInfo = [];
// record liver's info (address, name, avatar, ...); the array of Aliver class

var videoInst = {};
// video instances

var newLiver;
// new liver instance to be added